{
  "name": "dockpit",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "dev:api": "bun run --filter @dockpit/api dev",
    "dev:web": "bun run --filter @dockpit/web dev"
  }
}
